describe("createEmptyReport", () => {
  test("throw error if reportData is not an object", async () => {});
  test("throw error if reportData doesn't have employeeId", async () => {});
  test("return aa report if reportData is valid", async () => {});
  test("return aa report if reportData doesn't contain all values", async () => {});
});
