

describe("createShiftSchedule", () => {
    test.todo("throw error if startTime is invalid");
    test.todo("throw error if endTime is invalid");
    test.todo("throw error if employeeId is invalid/not available");
    test.todo("return shift schedule if create successfully");
});